const name = "Freddy";
typeof name === "strng";

if (!"serviceWorker" in navigator) {
  // old browser
}

const greeting = "hello";
console
  .log(`${greting} world`)

  [(1, 2, 3)].forEach(x => console.log(x));
